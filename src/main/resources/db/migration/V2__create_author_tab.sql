CREATE TABLE COOKBOOK.AUTHOR_TAB
(
    id                          VARCHAR(50) NOT NULL,
    name                        VARCHAR(150) NOT NULL,
    surname                     VARCHAR(150) NOT NULL,
    PRIMARY KEY (id)
);